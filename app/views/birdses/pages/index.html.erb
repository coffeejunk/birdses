<%= render 'nav' %>
<h1>Pages</h1>
<ul>
  <% @pages.each do |page| %>
    <li>
      <%= link_to page.name, controller: 'pages',
                             action: :show, id: page.name %>
    </li>
  <% end %>
</ul>
